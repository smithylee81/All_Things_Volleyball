from django.apps import AppConfig

""" Apps has been copied and
    adapted from the CI's Boutique Ado project """
class ProductsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'products'
